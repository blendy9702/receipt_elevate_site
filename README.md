# 개발

```bash
cd /usr/local/receipt_server/frontend/receipt_elevate_site
npm run dev
```

- 주소: http://localhost:3002
- 로그인 후 popcorn FastAPI (`/api/management/*`, `/api/tickets/*`, `/api/notifications/*`)로 대시보드 표시
- 상시 서비스 없음 — `npm run dev`로만 실행
